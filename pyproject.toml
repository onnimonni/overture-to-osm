[project]
name = "overture-to-osm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "country-bounding-boxes>=0.2.3",
    "duckdb>=1.3.2",
]
